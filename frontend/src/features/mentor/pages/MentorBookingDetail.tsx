import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { useState } from "react";
import { Link, useParams } from "react-router-dom";
import { bookingsApi } from "@/shared/api/resources";
import AuthNavbar from "@/shared/components/AuthNavbar";
import ErrorPanel from "@/shared/components/ErrorPanel";
export default function MentorBookingDetail() { const { bookingId = "" } = useParams(); const queryClient = useQueryClient(); const booking = useQuery({ queryKey: ["booking", bookingId], queryFn: () => bookingsApi.get(bookingId) }); const [reason, setReason] = useState(""); const transition = useMutation({ mutationFn: (action: string) => bookingsApi.transition(bookingId, { action, version: booking.data!.version, ...(["REJECT", "CANCEL"].includes(action) ? { reason: reason || "Mentor decision" } : {}) }), onSuccess: (data) => queryClient.setQueryData(["booking", bookingId], data) }); return <div className="min-h-screen bg-canvas"><AuthNavbar /><main className="mx-auto max-w-[800px] px-6 py-8">{booking.error ? <ErrorPanel error={booking.error} onRetry={() => booking.refetch()} /> : booking.isLoading ? <p className="text-sm text-ink-muted">Đang tải booking…</p> : booking.data && <><section className="rounded-xl border border-edge bg-panel p-6"><div className="flex justify-between"><h1 className="text-lg font-semibold text-ink">Yêu cầu từ {booking.data.studentName}</h1><span className="text-xs font-semibold text-primary">{booking.data.status}</span></div><p className="mt-4 text-sm text-ink-secondary">{booking.data.goal}</p><p className="mt-2 text-xs text-ink-muted">{new Date(booking.data.startsAt).toLocaleString("vi-VN")} · {booking.data.interviewType}</p>{booking.data.correctedText && <details className="mt-5 rounded-lg bg-canvas-subtle p-4"><summary className="cursor-pointer text-xs font-semibold text-ink">Corrected JD text được phép chia sẻ</summary><p className="mt-3 whitespace-pre-wrap text-xs leading-5 text-ink-secondary">{booking.data.correctedText}</p></details>}</section>{transition.error && <div className="mt-5"><ErrorPanel error={transition.error} /></div>}<section className="mt-5 rounded-xl border border-edge bg-panel p-5"><label className="block text-xs font-semibold text-ink-secondary">Lý do quyết định<textarea value={reason} onChange={(event) => setReason(event.target.value)} className="mt-1.5 w-full rounded-md border border-edge p-3 text-sm" /></label><div className="mt-4 flex flex-wrap gap-2">{booking.data.status === "PENDING" && <><button onClick={() => transition.mutate("CONFIRM")} className="rounded-md bg-primary px-4 py-2 text-xs font-medium text-on-primary">Xác nhận</button><button onClick={() => transition.mutate("REJECT")} className="rounded-md border border-danger/30 px-4 py-2 text-xs font-medium text-danger">Từ chối</button></>}{booking.data.status === "CONFIRMED" && <><Link to={`/mentor/sessions/${booking.data.id}`} className="rounded-md bg-primary px-4 py-2 text-xs font-medium text-on-primary">Quản lý buổi phỏng vấn</Link><button onClick={() => transition.mutate("COMPLETE")} className="rounded-md border border-edge px-4 py-2 text-xs font-medium text-ink-secondary">Đánh dấu hoàn tất</button></>}{booking.data.status === "COMPLETED" && <Link to={`/mentor/feedback/${booking.data.id}`} className="rounded-md bg-primary px-4 py-2 text-xs font-medium text-on-primary">Gửi feedback</Link>}</div></section></>}</main></div>; }
