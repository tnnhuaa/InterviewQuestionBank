import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { Link, useParams } from "react-router-dom";
import { bookingsApi } from "@/shared/api/resources";
import AuthNavbar from "@/shared/components/AuthNavbar";
import ErrorPanel from "@/shared/components/ErrorPanel";

export default function BookingStatus() {
  const { bookingId = "" } = useParams(); const queryClient = useQueryClient();
  const booking = useQuery({ queryKey: ["booking", bookingId], queryFn: () => bookingsApi.get(bookingId), enabled: Boolean(bookingId), refetchInterval: (query) => ["PENDING", "RESCHEDULE_PROPOSED"].includes(query.state.data?.status ?? "") ? 10_000 : false });
  const transition = useMutation({ mutationFn: (input: { action: string; reason?: string }) => bookingsApi.transition(bookingId, { ...input, version: booking.data!.version }), onSuccess: (data) => queryClient.setQueryData(["booking", bookingId], data) });
  return <div className="min-h-screen bg-canvas"><AuthNavbar /><main className="mx-auto max-w-[760px] px-6 py-8">{booking.error ? <ErrorPanel error={booking.error} onRetry={() => booking.refetch()} /> : booking.isLoading ? <p className="text-sm text-ink-muted">Đang tải lịch hẹn…</p> : booking.data && <><section className="rounded-xl border border-edge bg-panel p-6"><div className="flex items-start justify-between gap-3"><div><p className="text-xs text-ink-muted">Mã đặt lịch</p><h1 className="mt-1 font-mono text-sm font-semibold text-ink">{booking.data.id}</h1></div><span className="rounded-full bg-primary-soft px-3 py-1 text-xs font-semibold text-primary">{booking.data.status}</span></div><dl className="mt-6 grid gap-3 sm:grid-cols-2"><div><dt className="text-xs text-ink-muted">Mentor</dt><dd className="mt-1 text-sm font-medium text-ink">{booking.data.mentorName}</dd></div><div><dt className="text-xs text-ink-muted">Thời gian</dt><dd className="mt-1 text-sm font-medium text-ink">{new Date(booking.data.startsAt).toLocaleString("vi-VN")}</dd></div><div><dt className="text-xs text-ink-muted">Loại</dt><dd className="mt-1 text-sm text-ink">{booking.data.interviewType}</dd></div><div><dt className="text-xs text-ink-muted">Mục tiêu</dt><dd className="mt-1 text-sm text-ink">{booking.data.goal}</dd></div></dl>{booking.data.meetingLink && <a href={booking.data.meetingLink} target="_blank" rel="noreferrer" className="mt-5 inline-block rounded-md bg-primary px-4 py-2 text-sm font-medium text-on-primary">Mở phòng phỏng vấn</a>}</section>{transition.error && <div className="mt-5"><ErrorPanel error={transition.error} onRetry={() => transition.mutate(transition.variables!)} /></div>}<section className="mt-5 rounded-xl border border-edge bg-panel p-5"><h2 className="text-sm font-semibold text-ink">Thao tác</h2><div className="mt-3 flex flex-wrap gap-2">{["PENDING", "CONFIRMED"].includes(booking.data.status) && <button disabled={transition.isPending} onClick={() => transition.mutate({ action: "CANCEL", reason: "Student requested cancellation" })} className="rounded-md border border-danger/30 px-4 py-2 text-xs font-medium text-danger">Yêu cầu hủy</button>}{booking.data.status === "RESCHEDULE_PROPOSED" && <><button onClick={() => transition.mutate({ action: "ACCEPT_RESCHEDULE" })} className="rounded-md bg-primary px-4 py-2 text-xs font-medium text-on-primary">Chấp nhận giờ mới</button><button onClick={() => transition.mutate({ action: "REJECT_RESCHEDULE" })} className="rounded-md border border-edge px-4 py-2 text-xs font-medium text-ink-secondary">Từ chối</button></>}{booking.data.status === "COMPLETED" && <><Link to={`/bookings/${booking.data.id}/feedback`} className="rounded-md border border-edge px-4 py-2 text-xs font-medium text-ink-secondary">Xem feedback</Link><Link to={`/bookings/${booking.data.id}/review`} className="rounded-md bg-primary px-4 py-2 text-xs font-medium text-on-primary">Viết review</Link></>}</div>{booking.data.operationCase && <p className="mt-4 rounded-lg bg-notice-soft p-3 text-xs text-notice-ink">Case vận hành: {booking.data.operationCase.id}. Trạng thái booking chưa bị thay đổi tùy ý.</p>}</section></>}</main></div>;
}
