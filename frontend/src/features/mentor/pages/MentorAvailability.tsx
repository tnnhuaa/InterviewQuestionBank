import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { useState } from "react";
import { mentorsApi } from "@/shared/api/resources";
import AuthNavbar from "@/shared/components/AuthNavbar";
import ErrorPanel from "@/shared/components/ErrorPanel";
export default function MentorAvailability() { const queryClient = useQueryClient(); const slots = useQuery({ queryKey: ["availability"], queryFn: mentorsApi.slots }); const [startsAt, setStartsAt] = useState(""); const [endsAt, setEndsAt] = useState(""); const timezone = Intl.DateTimeFormat().resolvedOptions().timeZone; const create = useMutation({ mutationFn: () => mentorsApi.createSlot({ startsAt: new Date(startsAt).toISOString(), endsAt: new Date(endsAt).toISOString(), timezone }), onSuccess: () => queryClient.invalidateQueries({ queryKey: ["availability"] }) }); const remove = useMutation({ mutationFn: ({ id, version }: { id: string; version: number }) => mentorsApi.cancelSlot(id, version), onSuccess: () => queryClient.invalidateQueries({ queryKey: ["availability"] }) }); return <div className="min-h-screen bg-canvas"><AuthNavbar /><main className="mx-auto max-w-[850px] px-6 py-8"><h1 className="text-[22px] font-semibold text-ink">Lịch rảnh</h1><p className="mt-1 text-sm text-ink-secondary">Lưu UTC cùng múi giờ nguồn; slot quá khứ và chồng lấn sẽ bị từ chối.</p>{(slots.error || create.error || remove.error) && <div className="mt-5"><ErrorPanel error={slots.error || create.error || remove.error} /></div>}<form onSubmit={(event) => { event.preventDefault(); create.mutate(); }} className="mt-6 grid gap-3 rounded-xl border border-edge bg-panel p-5 sm:grid-cols-[1fr_1fr_auto]"><label className="text-xs font-semibold text-ink-secondary">Bắt đầu<input required type="datetime-local" value={startsAt} onChange={(event) => setStartsAt(event.target.value)} className="mt-1 block w-full rounded-md border border-edge px-3 py-2 text-sm" /></label><label className="text-xs font-semibold text-ink-secondary">Kết thúc<input required type="datetime-local" value={endsAt} onChange={(event) => setEndsAt(event.target.value)} className="mt-1 block w-full rounded-md border border-edge px-3 py-2 text-sm" /></label><button className="self-end rounded-md bg-primary px-4 py-2.5 text-sm font-medium text-on-primary">Thêm slot</button></form><div className="mt-6 space-y-3">{slots.data?.items.map((slot) => <article key={slot.id} className="flex items-center justify-between gap-4 rounded-xl border border-edge bg-panel p-4"><div><p className="text-sm font-medium text-ink">{new Date(slot.startsAt).toLocaleString("vi-VN")}</p><p className="mt-1 text-xs text-ink-muted">{slot.timezone}</p></div>{"version" in slot && "status" in slot && slot.status === "AVAILABLE" && <button onClick={() => remove.mutate({ id: slot.id, version: Number(slot.version) })} className="text-xs font-medium text-danger">Xóa</button>}</article>)}</div></main></div>; }
