import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { useState } from "react";
import { useParams } from "react-router-dom";
import { bookingsApi } from "@/shared/api/resources";
import AuthNavbar from "@/shared/components/AuthNavbar";
import ErrorPanel from "@/shared/components/ErrorPanel";
export default function MentorSessionDetail() { const { sessionId = "" } = useParams(); const queryClient = useQueryClient(); const booking = useQuery({ queryKey: ["booking", sessionId], queryFn: () => bookingsApi.get(sessionId) }); const [url, setUrl] = useState(""); const mutation = useMutation({ mutationFn: () => bookingsApi.meetingLink(sessionId, { url, version: booking.data?.meetingLinkVersion }), onSuccess: () => queryClient.invalidateQueries({ queryKey: ["booking", sessionId] }) }); return <div className="min-h-screen bg-canvas"><AuthNavbar /><main className="mx-auto max-w-[720px] px-6 py-8"><h1 className="text-[22px] font-semibold text-ink">Phòng phỏng vấn</h1><p className="mt-1 text-sm text-ink-secondary">Link được mã hóa trong database và chỉ hai bên xem khi booking CONFIRMED.</p>{(booking.error || mutation.error) && <div className="mt-5"><ErrorPanel error={booking.error || mutation.error} /></div>}<form onSubmit={(event) => { event.preventDefault(); mutation.mutate(); }} className="mt-6 rounded-xl border border-edge bg-panel p-6"><label className="block text-xs font-semibold text-ink-secondary">HTTPS meeting link<input required type="url" pattern="https://.*" value={url} onChange={(event) => setUrl(event.target.value)} placeholder="https://meet.example.com/..." className="mt-1.5 w-full rounded-lg border border-edge px-4 py-2.5 text-sm" /></label><button className="mt-4 w-full rounded-lg bg-primary px-5 py-3 text-sm font-medium text-on-primary">Lưu link</button></form></main></div>; }
