import { useQuery } from "@tanstack/react-query";
import { Link } from "react-router-dom";
import { bookingsApi } from "@/shared/api/resources";
import AuthNavbar from "@/shared/components/AuthNavbar";
import ErrorPanel from "@/shared/components/ErrorPanel";
export default function MentorBookings() { const bookings = useQuery({ queryKey: ["bookings"], queryFn: () => bookingsApi.list({ pageSize: 50 }) }); return <div className="min-h-screen bg-canvas"><AuthNavbar /><main className="mx-auto max-w-[900px] px-6 py-8"><h1 className="text-[22px] font-semibold text-ink">Booking Inbox</h1><p className="mt-1 text-sm text-ink-secondary">Các quyết định luôn dùng version mới nhất; không optimistic update.</p>{bookings.error && <div className="mt-5"><ErrorPanel error={bookings.error} onRetry={() => bookings.refetch()} /></div>}<div className="mt-6 space-y-3">{bookings.data?.items.map((booking) => <Link key={booking.id} to={`/mentor/bookings/${booking.id}`} className="flex items-center justify-between gap-4 rounded-xl border border-edge bg-panel p-5 hover:border-primary/30"><div><p className="text-sm font-semibold text-ink">{booking.studentName} · {booking.interviewType}</p><p className="mt-1 text-xs text-ink-muted">{new Date(booking.startsAt).toLocaleString("vi-VN")}</p></div><span className="rounded-full bg-primary-soft px-3 py-1 text-xs font-medium text-primary">{booking.status}</span></Link>)}{bookings.data?.items.length === 0 && <p className="rounded-xl border border-dashed border-edge p-10 text-center text-sm text-ink-muted">Chưa có booking.</p>}</div></main></div>; }
